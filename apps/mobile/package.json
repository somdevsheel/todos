{
  "name": "@arutech/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Arutech Workspace Android app — React Native + Expo. Phase 1 is a placeholder screen only; auth/tasks/chat/FCM/deep-linking land in Phase 6 (see ANDROID.md).",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "lint": "echo \"(mobile) no lint configured for the Phase 1 placeholder\" && exit 0",
    "test": "echo \"(mobile) no tests until Phase 6 builds real screens\" && exit 0"
  },
  "dependencies": {
    "expo": "^57.0.13",
    "expo-status-bar": "^57.0.1",
    "react": "^19.2.8",
    "react-native": "0.86.2"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@types/react": "^19.2.18",
    "babel-preset-expo": "^57.0.7",
    "typescript": "^5.9.3"
  }
}
