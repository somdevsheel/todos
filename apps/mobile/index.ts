import { registerRootComponent } from "expo";
import App from "./App";

// registerRootComponent calls AppRegistry.registerComponent('main', () => App)
// and sets up the environment appropriately for Expo, whether running in
// Expo Go, a development build, or a production build of the app.
registerRootComponent(App);
