# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.next/
out/
*.tsbuildinfo

# env
.env
.env.local
.env.production
.env.*.local
!.env.example
!.env.production.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# testing
coverage/

# database backups (scripts/backup-postgres.sh's output) — real data, never committed
/backups/

# prisma
apps/api/prisma/migrations/dev.db

# IDE / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# expo / react-native
apps/mobile/.expo/
apps/mobile/.expo-shared/
apps/mobile/android/
apps/mobile/ios/

# Firebase Android app config (real Firebase project registration — see
# ANDROID.md's "Real device build" section for how to get a fresh copy from
# Firebase Console). Its api_key is technically safe to ship inside a
# compiled APK by design, but kept out of the repo's source anyway — same
# treat-every-credential-cautiously discipline as everything else here.
# apps/mobile/google-services.json

# docker volumes (bind-mounted local data, if any)
docker/.data/

# local-disk file storage (dev only — see STORAGE_LOCAL_DIR in .env.example)
apps/api/uploads/
apps/mobile/google-services.json